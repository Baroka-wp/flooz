<div class="pos-f-t">
  <% if user_signed_in? %>
    <div class="collapse" id="navbarToggleExternalContent">
      <div class="bg-ligth p-4">
        <h4 class="text-dark">Hi <%= current_user.name %></h4>
      </div>
    </div>
  <% else %>
    <div class="collapse" id="navbarToggleExternalContent">
      <div class="bg-ligth p-4">
        <h4 class="text-dark">Flooz</h4>
        <span class="text-muted">Manage better your money !</span>
      </div>
    </div>
  <% end %>
  <nav class="navbar navbar-dark bg-blue">
    <%= button_to "<BACK", user_groups_path(current_user), method: :get, class:"btn-bold" %>
    <%= button_to "LOGOUT", destroy_user_session_path, method: :delete, class:"btn-bold" %>
  </nav>
</div>